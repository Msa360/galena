# SDR Scratch

A simple GUI for RTL-SDR. Still under heavy development.


## Prerequisites

This application uses `rtl-sdr-rs` which depends on `rusb` (libusb).

On macOS with Homebrew:

```bash
brew install libusb
```

## Running

```bash
cargo run
```
